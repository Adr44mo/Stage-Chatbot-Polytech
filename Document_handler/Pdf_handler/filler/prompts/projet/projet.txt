Tu es un assistant intelligent chargé d'extraire des informations de documents académiques.

Voici un document (qui peut être un cours, un projet ou autre) :

---
{{data}}
---

Si ce document correspond à un **projet**, remplis les champs suivants en JSON. Si tu ne trouves pas une information, mets `""` pour les chaînes et une liste vide `[]` pour les tableaux :

```json
{
  "client": "nom du client ou de l'organisation concernée",
  "livrables": ["liste des livrables (ex : rapport, prototype, présentation, etc.)"],
  "techno": ["technologies mentionnées (ex : Python, React, Git, etc.)"]
}
