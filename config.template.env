# =============================================================================
# Configuration Template pour Stage-Chatbot-Polytech
# =============================================================================
# Copiez ce fichier vers config.env et remplissez les valeurs
# Le script init.sh utilisera ce fichier pour générer les .env nécessaires

# =============================================================================
# CONFIGURATION OPENAI
# =============================================================================
OPENAI_API_KEY=your_openai_api_key_here

# =============================================================================
# CONFIGURATION RECAPTCHA
# =============================================================================
RECAPTCHA_SECRET_KEY=your_recaptcha_secret_key_here
RECAPTCHA_SITE_KEY=your_recaptcha_site_key_here

# =============================================================================
# CONFIGURATION SERVEUR
# =============================================================================
# Domaine ou IP du serveur (pour les certificats SSL)
SERVER_DOMAIN=localhost

# Ports internes (nginx exposera seulement 443 à l'extérieur)
BACKEND_PORT=8000
FRONTEND_PORT=5173

# =============================================================================
# CONFIGURATION SSL (pour HTTPS)
# =============================================================================
# Laisser vide pour génération automatique de certificats auto-signés
SSL_CERT_PATH=
SSL_KEY_PATH=
SSL_COUNTRY=FR
SSL_STATE=France
SSL_CITY=Paris
SSL_ORGANIZATION=Polytech
SSL_UNIT=IT
SSL_EMAIL=admin@polytech.fr

# =============================================================================
# CONFIGURATION ENVIRONNEMENT
# =============================================================================
# Mode de déploiement (development/production)
DEPLOYMENT_MODE=development

# =============================================================================
# CONFIGURATION PRODUCTION
# =============================================================================
# Nombre de workers uvicorn pour production (par défaut: 2)
WORKERS=2

# Intervalle de redémarrage backend en secondes (par défaut: 3600 = 1h)
# 3600 = 1 heure, 7200 = 2 heures, 1800 = 30 minutes
RESTART_INTERVAL=3600

# Log level pour uvicorn (debug, info, warning, error, critical)
LOG_LEVEL=info

# =============================================================================
# CONFIGURATION SYSTÈME
# =============================================================================
# Répertoire de travail
WORK_DIR=/srv/partage/Stage-Chatbot-Polytech

# Python version minimum requise
PYTHON_MIN_VERSION=3.12

# =============================================================================
# NOTES IMPORTANTES
# =============================================================================
# - Le script start-production.sh lit toutes ces variables
# - Modifiez ces valeurs selon vos besoins avant de lancer la production
# - Pour appliquer les changements, relancez ./start-production.sh
# - Utilisez ./monitor.sh pour surveiller l'état du système
